import { View } from 'react-native';

// This screen is never navigated to directly.
// The "More" tab button renders a floating bubble menu instead.
export default function MoreScreen() {
  return <View />;
}
